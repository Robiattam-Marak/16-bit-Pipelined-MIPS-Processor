`timescale 1ns/1ps

module alu_control_unit_tb;

    //--------------------------------------------------
    // INPUTS
    //--------------------------------------------------
    reg [4:0] opcode;
    reg [1:0] func;

    //--------------------------------------------------
    // OUTPUTS
    //--------------------------------------------------
    wire Carry_In;
    wire Ainvert;
    wire Binvert;

    wire [2:0] Operation;
    wire [1:0] ResCtrl;
    wire [1:0] ShfCtrl;

    wire Jr;

    //--------------------------------------------------
    // DUT
    //--------------------------------------------------
    alu_control_unit DUT(
        .opcode(opcode),
        .func(func),

        .Carry_In(Carry_In),
        .Ainvert(Ainvert),
        .Binvert(Binvert),

        .Operation(Operation),
        .ResCtrl(ResCtrl),
        .ShfCtrl(ShfCtrl),

        .Jr(Jr)
    );

    //--------------------------------------------------
    // DISPLAY TASK
    //--------------------------------------------------
    task show;
    begin
        $display("------------------------------------------");
        $display("TIME      = %0t", $time);
        $display("OPCODE    = %0d", opcode);
        $display("FUNC      = %0d", func);

        $display("Carry_In  = %b", Carry_In);
        $display("Ainvert   = %b", Ainvert);
        $display("Binvert   = %b", Binvert);

        $display("Operation = %b", Operation);
        $display("ResCtrl   = %b", ResCtrl);
        $display("ShfCtrl   = %b", ShfCtrl);

        $display("Jr        = %b", Jr);
        $display("------------------------------------------\n");
    end
    endtask

    //--------------------------------------------------
    // TESTS
    //--------------------------------------------------
    initial begin

        //----------------------------------
        // AND
        //----------------------------------
        opcode = 0; func = 0;
        #10;
        $display("AND");
        show();

        //----------------------------------
        // OR
        //----------------------------------
        opcode = 0; func = 1;
        #10;
        $display("OR");
        show();

        //----------------------------------
        // XOR
        //----------------------------------
        opcode = 0; func = 2;
        #10;
        $display("XOR");
        show();

        //----------------------------------
        // NOR
        //----------------------------------
        opcode = 0; func = 3;
        #10;
        $display("NOR");
        show();

        //----------------------------------
        // ADD
        //----------------------------------
        opcode = 1; func = 0;
        #10;
        $display("ADD");
        show();

        //----------------------------------
        // SUB
        //----------------------------------
        opcode = 1; func = 1;
        #10;
        $display("SUB");
        show();

        //----------------------------------
        // SLT
        //----------------------------------
        opcode = 1; func = 2;
        #10;
        $display("SLT");
        show();

        //----------------------------------
        // SLTU
        //----------------------------------
        opcode = 1; func = 3;
        #10;
        $display("SLTU");
        show();

        //----------------------------------
        // JR
        //----------------------------------
        opcode = 2; func = 0;
        #10;
        $display("JR");
        show();

        //----------------------------------
        // ANDI
        //----------------------------------
        opcode = 4; func = 0;
        #10;
        $display("ANDI");
        show();

        //----------------------------------
        // ORI
        //----------------------------------
        opcode = 5; func = 0;
        #10;
        $display("ORI");
        show();

        //----------------------------------
        // XORI
        //----------------------------------
        opcode = 6; func = 0;
        #10;
        $display("XORI");
        show();

        //----------------------------------
        // ADDI
        //----------------------------------
        opcode = 7; func = 0;
        #10;
        $display("ADDI");
        show();

        //----------------------------------
        // SLL
        //----------------------------------
        opcode = 8;
        #10;
        $display("SLL");
        show();

        //----------------------------------
        // SRL
        //----------------------------------
        opcode = 9;
        #10;
        $display("SRL");
        show();

        //----------------------------------
        // SRA
        //----------------------------------
        opcode = 10;
        #10;
        $display("SRA");
        show();

        //----------------------------------
        // ROR
        //----------------------------------
        opcode = 11;
        #10;
        $display("ROR");
        show();

        //----------------------------------
        // LW
        //----------------------------------
        opcode = 12;
        #10;
        $display("LW");
        show();

        //----------------------------------
        // SW
        //----------------------------------
        opcode = 13;
        #10;
        $display("SW");
        show();

        //----------------------------------
        // BEQ
        //----------------------------------
        opcode = 14;
        #10;
        $display("BEQ");
        show();

        //----------------------------------
        // BNE
        //----------------------------------
        opcode = 15;
        #10;
        $display("BNE");
        show();

        //----------------------------------
        // BLT
        //----------------------------------
        opcode = 16;
        #10;
        $display("BLT");
        show();

        //----------------------------------
        // BGE
        //----------------------------------
        opcode = 17;
        #10;
        $display("BGE");
        show();

        $display("===== TEST COMPLETE =====");

        #10;
        $finish;

    end

endmodule